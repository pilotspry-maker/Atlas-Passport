# Atlas-Passport · Engine Sweep Handoff

**Date:** 2026-06-27 (post-launch +1d)
**PR:** [#29 lockdown(engine-sweep)](https://github.com/pilotspry-maker/Atlas-Passport/pull/29)
**Production:** https://atlas-passport.vercel.app · deploy `dpl_DuoR9qyxvJLyWCnYTP7Uhgq6fJab` (healthy)
**Public status page:** https://atlas-passport.pplx.app
**Local ops dashboard:** `/home/user/workspace/atlas-ops/`

---

## What was done (autonomous)

### Security
- Found and patched **critical anon-mint exploit**: `public.ap_events` and `public.referral_events` had `WITH CHECK (true)` INSERT policies on role `{public}`. Any client with the publishable key could mint AP and forge referrals. Migration 024 § A replaces both with `auth.role() = 'service_role'`.
- Pinned `search_path` on 3 SECURITY DEFINER functions (024 § B).
- Wrapped `auth.uid()` in `(select ...)` across 13 policies for plan caching (024 § C).
- Added 16 covering FK indexes (024 § D).
- Added service_role policies to `corridor.audit_log` + `corridor.jobs` (024 § E).
- Replaced the broad `corridor-covers` bucket SELECT with name-required SELECT (024 § F).

### Drift recovery
- Source had migrations up to 019; prod had 023. Recovered 020-023 SQL bodies from `supabase_migrations.schema_migrations.statements` and committed them.

### Key rotation
- Vercel **production** env rotated: `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` now use the publishable key (`sb_publishable_…`).
- Force redeploy completed in 44s; HTTP 200; no JWT bundled in HTML.
- `.env.old` snapshot was created and then shredded.

### Dependencies (audit only — no changes)
- Memory's launch-blocker CVE list was stale. Current `package.json` already pins `next ^16.2.9`, `ws 8.21.0`, `cookie 1.1.1`, `prismjs 1.30.0` — all four CVEs are resolved.

### Dashboards
- **Public:** `https://atlas-passport.pplx.app` — read-only network status using the publishable key under RLS. Shows active corridors, nodes, and waitlist cities. CI / test fixtures filtered out client-side.
- **Local ops:** `/home/user/workspace/atlas-ops/dashboard.mjs` — service-role-backed terminal dashboard. Funnel (waitlist → traveler_profiles → activations → check_ins → AP), per-corridor breakdown, AP economy, referrals, env health.

---

## Apply order (your action)

1. **Review and merge PR #29.** CI must pass:
   - RLS Exploit Tests (24 assertions)
   - RLS Regression Tests (reg-01 → reg-04)
2. After merge, Supabase migration runner applies 020-024 in order. Dry-run on prod (`BEGIN; … ROLLBACK;`) executed cleanly, so apply should be uneventful.
3. **Disable the legacy JWT anon key** in Supabase dashboard (Settings → API → Legacy keys → Disable). It is still active. The publishable key is the only thing that should remain.
4. **Add `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` to Vercel Preview env.** CLI couldn't answer the interactive branch prompt non-interactively. Use the Vercel dashboard:
   - Settings → Environment Variables → Add → Environment: Preview
   - Use the same publishable-key values that are in Production.
5. **(Optional)** Set `SUPABASE_SERVICE_ROLE_KEY` in Vercel Production with a fresh service-role key from Supabase. It still holds the placeholder string.
6. Run the local ops dashboard once with the fresh service-role key:
   ```bash
   cd /home/user/workspace/atlas-ops
   cp .env.example .env  # paste your service-role key
   node dashboard.mjs
   ```

---

## Known follow-ups (out of scope this sweep)

- **Two parallel player models.** `traveler_profiles` (PK → `auth.users`) and `passports` (`user_id` → `public.profiles` → `auth.users`) both exist. `check_ins` references both (`traveler_id` AND `user_id`, `passport_id` AND `activation_id`). One model should be deprecated.
- **Preview env vars.** Still empty for the Supabase URL + anon key in Vercel Preview environment.
- **`dev_env_status` table** is empty. Used for the local ops health panel — populate it as part of CI seed if you want active health signal.

---

## Files of record

| Path | Purpose |
| --- | --- |
| `supabase/migrations/020_…sql` → `024_…sql` | Drift recovery + lockdown |
| `docs/ENGINE_SWEEP_2026-06-27.md` | Full audit report |
| `docs/HANDOFF_2026-06-27.md` | This handoff |
| `/home/user/workspace/atlas-ops/` | Local-only ops dashboard (service-role) |
| `/home/user/workspace/atlas-public-dashboard/` | Source for atlas-passport.pplx.app |
